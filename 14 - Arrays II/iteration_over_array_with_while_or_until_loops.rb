animals = ["Lion", "Zebra", "Baboon", "Cheetah"]

count = 0
while count < animals.length
    puts "The index is #{count} and the animal is #{animals[count]}"
    count += 1
end

until i == animals.length
    puts "The index is #{count} and the animal is #{animals[count]}"
    count += 1
end
